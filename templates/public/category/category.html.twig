{% extends 'base.html.twig' %}

{% block title %}{% endblock %}

{% block body %}
    <h1 class="font-semibold">{{ name }}</h1>
    {% for post in blogPosts %}
        <p>{{ post.title }}</p>
    {% endfor %}
{% endblock %}
